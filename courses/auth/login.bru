meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{host}}login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "danek@danek.com",
    "password": "danekk"
  }
}

script:post-response {
  bru.setEnvVar("token",res.body.token)
}

settings {
  encodeUrl: true
}
